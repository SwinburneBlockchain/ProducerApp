<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.swinblockchain.producerapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>

    <application
        android:allowBackup="true"
        android:name=".App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScanQR.ScanActivity" />
        <activity android:name=".GenQR.QueryForQRActivity" />
        <activity android:name=".GenQR.DisplayQRCodeActivity" />
        <activity android:name=".GenQR.QRCodeParametersActivity" />
        <activity android:name=".GenQR.ScanProducerQR" />
        <activity android:name=".ScanQR.LocationParameterActivity" />
        <activity android:name=".AcknowledgementsActivity" />
        <activity android:name=".ScanQR.ProofOfLocation"></activity>
    </application>

</manifest>